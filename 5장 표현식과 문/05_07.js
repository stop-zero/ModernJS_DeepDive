// 표현식 : 리터럴, 식별자, 연산자, 함수 호출

// 숫자 리터럴 3
3
// 리터럴 표현식
10;
('Hello');

// 식별자 표현식(이미 선언되었다고 가정)
sum;
person.name;
arr[1];

// 연산자 표현식
1 + 2;
sum = 3;
sum !== 3;

// 함수 / 메서드 호출 표현식(이미 선언되었다고 가정)
square();
person.getName();
